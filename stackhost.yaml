# StackHost configuration file
# Version: 2.0.0

runtime:
  image: node:22

commands:
  build:
    - npm install
  start: node index.js

repository:
  branch: main
  auto_deploy: true

env:
  - PORT:3000
  - CFIP:saas.sin.fan
  - CFPORT:443
